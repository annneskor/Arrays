# Работа с массивами в C

Проект объединяет лабораторные работы №11, №14, №16, №17 и домашние задания по курсу программирования на языке C.  

---

## 📋 Задания к лаб. 11

1. Табулирование функции `f(x) = x - 3·cos²(1.04x)` на отрезке `[0.15, 2.1]` с шагом.
2. Вычисление суммы положительных и отрицательных элементов.
3. Подсчёт количества положительных и отрицательных значений.
4. Нахождение среднего арифметического положительных элементов.
5. **ДЗ**: Работа с целочисленным массивом — поиск нулевых элементов, подсчёт количества и произведения элементов между первым/последним и первым/вторым нулём.

---

## 📋 Задания к лаб. 14

1. Заполнение массива значениями функции `F(x) = x - 3·cos²(1.04x)`.
2. Печать массива.
3. Преобразование массива (вычитание константы).
4. Вычисление суммы элементов в заданном диапазоне индексов.
5. Поиск элемента, равного заданному значению `a` (с учётом погрешности `EPS = 1e-4`).
6. Поиск минимального элемента среди значений, попадающих в интервал `(A, B)`.
7. Поиск индекса середины между первым и последним положительным / отрицательным / нулевым элементом.

---

## 📋 Задания к лаб. 16

1. Динамическое выделение памяти под массивы.
2. Заполнение массива случайными числами из диапазона `[-1, 1]`.
3. **Вставка**:
   - Элемента по заданному индексу.
   - Двух копий числа до и после минимального элемента.
4. **Удаление**:
   - Одного элемента по номеру.
   - Каждого `k`-го элемента.
   - `K` элементов, начиная с позиции `n`.
5. Корректное освобождение памяти.

---

## 📋 Задания к лаб. 17 (сортировки)

Реализованы четыре классических алгоритма сортировки для массивов типа `double`:

1. **Пузырьковая сортировка** (`bubble_sort`)
2. **Шейкерная (коктейльная) сортировка** (`shaker_sort`)
3. **Сортировка простым выбором** (`selection_sort`)
4. **Сортировка вставками** (`insertion_sort`)

Каждая сортировка тестируется на отдельной копии исходного массива.

---

## 📋 Домашнее задание к лаб. 16

1. Генерация трёх динамических массивов `A`, `B`, `C` размером от 10 до 50.
2. Формирование массива `D` путём вставки всего массива `A` **перед максимальным элементом** массива `C`.
3. Вывод всех четырёх массивов.

---

## 📁 Структура проекта

lab-arrays/
├── main.c # Точка входа, вызов всех задач
├── array.h # Заголовочный файл: объявления всех функций
└── array.c # Реализация всех функций

---

## 🔧 Реализованные функции

### 1. `double* full_elements(double *ptr_array, int n)`
- **Назначение**: Заполнение массива значениями функции `F(x)` на отрезке `[0.15, 2.1]`.
- **Параметры**: 
  - `ptr_array` — указатель на массив,
  - `n` — размер массива.
- **Возвращает**: указатель на заполненный массив.

### 2. `void put_elements(double *ptr_array, int n)`
- **Назначение**: Печать массива построчно с точностью до 4 знаков.
- **Параметры**: указатель на массив и его размер.

### 3. `double* calc_element(double *ptr_array, int n)`
- **Назначение**: Преобразование массива — вычитание константы `CONST_VAL = 1`.
- **Возвращает**: указатель на изменённый массив.

### 4. `double sum_elements(double *ptr_array, int begin, int end)`
- **Назначение**: Вычисление суммы элементов от индекса `begin` до `end` (включительно).

### 5. `int find_element(double *ptr_array, int n, double a)`
- **Назначение**: Поиск элемента, близкого к `a` (с точностью `1e-4`).
- **Возвращает**: индекс или `-1`, если не найден.

### 6. `double min_element(double *ptr_array, int n, double A, double B)`
- **Назначение**: Поиск минимального элемента среди значений в интервале `(A, B)`.
- **Возвращает**: значение минимума или `NAN`, если таких элементов нет.

### 7. `int find_index(double *ptr_array, int n, int type)`
- **Назначение**: Поиск индекса середины между первым и последним элементом заданного типа (1 — положительный, 2 — отрицательный, 3 — нулевой).

### 8. Функции управления памятью (`insert_1`, `insert_2`, `delete_1`, `delete_2`, `delete_3`)
- Реализуют динамическое изменение размера массива.

### 9. Сортировки (`bubble_sort`, `shaker_sort`, `selection_sort`, `insertion_sort`)
- Все функции работают in-place и принимают `double*` и размер.

---
